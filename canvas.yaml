preset: package

namespace: SmileyThane\LaravelJitsi
user-auth-provider: App\User

paths:
  src: src
  resource: resources

factory:
  path: database/factories

migration:
  path: database/migrations
  prefix: ''

console:
  namespace: SmileyThane\LaravelJitsi\Console

model:
  namespace: SmileyThane\LaravelJitsi

provider:
  namespace: SmileyThane\LaravelJitsi\Providers

testing:
  namespace: SmileyThane\LaravelJitsi\Tests
